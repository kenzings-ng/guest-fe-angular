import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-shipping-returns',
  imports: [RouterLink],
  templateUrl: './shipping-returns.html',
})
export class ShippingReturns {}
